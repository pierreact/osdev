#ifndef ISURUS_STRING_H
#define ISURUS_STRING_H

#include "types.h"

void *memcpy(void *dest, const void *src, size_t n);
void *memset(void *dest, int val, size_t n);
size_t strlen(const char *s);

#endif
